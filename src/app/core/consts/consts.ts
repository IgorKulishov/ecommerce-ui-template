export const languages = ['en', 'fr', 'ru'];
export const defaultLanguage = 'en';

// TODO: set defaultRole: undefined - role has to come from API (it's temp to test)
export const roles = ['seller', 'buyer', 'guest'];
export const defaultRole = 'seller';
